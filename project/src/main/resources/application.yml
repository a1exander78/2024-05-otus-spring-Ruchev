spring:
  h2:
    console.enabled: true

  datasource:
    url: jdbc:h2:mem:maindb
    username: sa
    password:

  jpa:
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml
    contexts: project

logging:
  level:
    ROOT: ERROR
